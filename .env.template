# Copy this file to `.env` and fill in your real values.
#   cp .env.template .env
# Any variable already exported in your shell takes precedence over `.env`.

# --- OpenRouter API (backend: openrouter) ---
OPENROUTER_API_KEY="Your OpenRouter API key here"

# Optional overrides (defaults shown; usually leave commented out).
# These match the defaults in configs.yaml -> llm_configs.general_configs.
# OPENROUTER_API_BASE="https://openrouter.ai/api/v1"
